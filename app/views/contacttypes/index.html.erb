<% # encoding: utf-8 %>
<% provide(:title, 'Типы контактов') %>

<h1>Типы контактов</h1>

<%= will_paginate %>

<table class="table table-bordered">
  <%= render @contacttypes %>
</table>

<%= will_paginate %>

<br>

<%= form_for(@contacttype, html: { class: 'form-horizontal' }) do |f| %>
    <%= render 'fields', f:f %>
    <div class="control-group">
		<div class="controls">
		    <%= f.submit "Добавить"  %>
		</div>
	</div>


<% end %>



