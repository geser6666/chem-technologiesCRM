<%= form_for(current_user.relationships.find_by_client_id(object),
             html: { method: :delete, class: "frm_follow" }, remote: true) do |f| %>
  <%= f.submit "Не получать", class: "btn btn-small" %>
<% end %>