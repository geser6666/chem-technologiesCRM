<%= form_for(current_user.relationships.build(client_id: object.id), html:{ class: "form-inline"}, remote: true) do |f| %>
<div class="form-group">
  <%= f.hidden_field :client_id %>
  <%= f.submit "Получать", class: "btn btn-default btn-xs" %>
<% end %>