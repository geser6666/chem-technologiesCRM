<%= form_for(current_user.relationships.build(client_id: object.id), html:{ class: ".form-inline"}) do |f| %>
  <%= f.hidden_field :client_id %>
  <%= f.submit "Получать", class: "btn btn-primary btn-small" %>
<% end %>