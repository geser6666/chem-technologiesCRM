<%= form_for(current_user.relationships.build(client_id: object.id), html:{ class: "frm_follow"}, remote: true) do |f| %>
  <%= f.hidden_field :client_id %>
  <%= f.submit "Получать", class: "btn btn-primary btn-small" %>
<% end %>