<li>
  <%= country.name %> <%= link_to '', edit_country_path(country.id),  class: "icon-edit" %>
  <%= link_to 'delete', country_path(country.id),  class: "icon-remove", method: :delete,
      data: { confirm: "You sure?" },
      title: country.name %>
</li>