<% provide(:title, 'Страны') %>
<h1>Список стран</h1>

<%= will_paginate %>

<table class="table table-hover col-md-2 ">
  <%= render @countries %>
</table>

<%= will_paginate %>
 
<br>


<%= form_for(@country, html: { class: 'form-horizontal' }) do |f| %>
    <%= render 'fields', f:f %>
    <div class="form-group">
		<div class="col-sm-2">
		    <%= f.submit "Добавить", class: 'btn btn-primary btn-xs' %>
		</div>
	</div>
<% end %>

